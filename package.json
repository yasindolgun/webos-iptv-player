{
  "name": "webos-iptv-player",
  "version": "1.15.0",
  "private": true,
  "license": "Apache-2.0",
  "comment:browserslist": "The gate's target = the FLOOR of the supported fleet: webOS 4 = Chromium 53. Drives stylelint/doiuse (CSS) and eslint-plugin-compat (JS APIs); esbuild uses its own 'chrome53' syntax target. A static gate can only check the floor (eslint-plugin-compat flags not-yet-added APIs, NOT ones removed by newer engines), so forward-compat on newer TVs is left to the Playwright e2e, which runs on a current Chromium.",
  "comment:@mdn/browser-compat-data": "Pinned exact, and declared even though eslint-plugin-compat already pulls it in: scripts/chromium-53-simulation.mjs derives the chromium-53-simulation removal set from it, so a floating range would change what the e2e suite deletes on any lockfile refresh, with no code change to review. Two versions exist in the tree (eslint-plugin-compat's and ast-metadata-inferer's); this makes which one we resolve explicit rather than a hoisting accident.",
  "browserslist": [
    "chrome 53"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit && node esbuild.config.mjs",
    "service:typecheck": "tsc -p bundled-service/tsconfig.json --noEmit",
    "service:build": "node scripts/clean-dir.mjs build/bundled-service && tsc -p bundled-service/tsconfig.json && node scripts/service-compat-gate.mjs build/bundled-service",
    "service:smoke": "npm run service:build && node scripts/run-service-smoke.mjs",
    "service:smoke:matrix": "npm run service:build && node scripts/run-service-smoke.mjs --matrix",
    "manifest": "node scripts/generate-homebrew-manifest.mjs",
    "package": "bash build.sh",
    "clean": "rm -rf dist build test-output *.ipk *.manifest.json",
    "preview": "npm run typecheck && node esbuild.config.mjs --preview && node scripts/serve.mjs",
    "screenshots": "node scripts/generate-screenshots.mjs",
    "lint": "npm run lint:css && npm run lint:js",
    "lint:css": "stylelint \"css/**/*.css\"",
    "lint:js": "eslint src",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:all": "npm run lint && vitest run && playwright test",
    "version": "node scripts/sync-version.mjs && git add appinfo.json",
    "benchmark:typecheck": "tsc -p benchmarks/tsconfig.json",
    "benchmark:parser-build": "node benchmarks/build-parser-bundle.mjs",
    "benchmark": "npm run typecheck && node esbuild.config.mjs --preview && npm run benchmark:typecheck && npm run benchmark:parser-build && playwright test --config benchmarks/playwright.config.ts",
    "benchmark:check": "npm run benchmark && node benchmarks/compare.mjs",
    "benchmark:update": "npm run benchmark && node benchmarks/compare.mjs --update",
    "benchmark:tv": "npm run benchmark:parser-build && node benchmarks/tv-runner.mjs",
    "benchmark:tv:check": "npm run benchmark:tv && node benchmarks/compare.mjs --tv",
    "benchmark:tv:update": "npm run benchmark:tv && node benchmarks/compare.mjs --tv --update",
    "benchmark:tv:cleanup": "node benchmarks/tv-runner.mjs --cleanup"
  },
  "devDependencies": {
    "@mdn/browser-compat-data": "6.1.5",
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.1.2",
    "@typescript-eslint/parser": "^8.65.0",
    "@vitest/coverage-v8": "4.1.10",
    "dashjs": "^5.2.1",
    "esbuild": "^0.28.1",
    "eslint": "^10.8.0",
    "eslint-plugin-compat": "^7.0.2",
    "fake-indexeddb": "^6.2.5",
    "hls.js": "^1.6.16",
    "jsdom": "^30.0.1",
    "mpegts.js": "^1.8.0",
    "postcss": "^8.5.25",
    "stylelint": "^17.14.1",
    "stylelint-no-unsupported-browser-features": "^8.1.1",
    "typescript": "^5.7.0",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "assjs": "^0.1.9",
    "fflate": "^0.8.3",
    "iso-639-1": "^3.1.6",
    "iso-639-2": "^3.0.2",
    "qrcode-generator": "^2.0.4"
  }
}
